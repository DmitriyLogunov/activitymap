import React, {useState} from "react";
import '../styles/activity_filter_form.css';

interface ActivityFilterFormProps {
  // selectionData: ActivitySelectionData;
  // onApplyClick: (newSelection: ActivitySelectionData) => void;
  onCancelClick: () => void;
}

const ActivityFilterForm = (props: ActivityFilterFormProps) => {
  <div className="activity-filter-form">Editor form</div>
}

export default ActivityFilterForm;
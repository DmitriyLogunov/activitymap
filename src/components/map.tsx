import React from 'react';

interface MapProps {
}

const Map = (props: MapProps) => {
  return (
    <div className="map">
      <p>Fetching Strava activities...</p>
    </div>
  );
}

export default Map;